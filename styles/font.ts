import { Inter, Rubik } from "next/font/google";

export const rubik = Rubik({
  subsets: ["latin"],
  weight: "400",
});

export const rubik_bold = Rubik({
  subsets: ["latin"],
  weight: "700",
});

export const inter = Inter({
  subsets: ["latin"],
  weight: "400",
});

export const inter_bold = Inter({
  subsets: ["latin"],
  weight: "700",
});
